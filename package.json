{
  "name": "n9-node-log",
  "version": "1.1.2",
  "description": "Logging module based on Winston for Neo9.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src"
  ],
  "scripts": {
    "build": "rimraf dist/ && tsc",
    "lint": "tslint src/*.ts",
    "test": "npm run lint && npm run build && nyc --reporter=lcov ava --verbose --serial dist/test/",
    "preversion": "npm test",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@scm.bytefactory.fr:2222/n9node/n9-node-log.git"
  },
  "author": "Sebastien Chopin <sebastien.chopin@neo9.fr>",
  "dependencies": {
    "@types/winston": "^2.3.2",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^3.0.0",
    "@types/nock": "^8.2.1",
    "@types/node": "^7.0.18",
    "ava": "^0.19.1",
    "fs-extra": "^3.0.1",
    "nock": "^9.0.13",
    "nyc": "^10.3.2",
    "rimraf": "^2.6.1",
    "std-mocks": "^1.0.1",
    "tmp-promise": "^1.0.3",
    "tslint": "^5.2.0",
    "typescript": "^2.3.2"
  }
}
